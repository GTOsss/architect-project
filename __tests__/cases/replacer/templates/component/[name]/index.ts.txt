import {{toCamelCase(name)}} from './{{name}}';

export default {{toCamelCase(name)}};

 const doChanges = 'do changes';
 const doChanges = 'do changes';
 const doChanges = 'do changes';
 const doChanges = 'do changes';
 const doChanges = 'do changes';
 const doChanges = 'do changes';
 const doChanges = 'do changes';
 const doChanges = 'do changes';
 const doChanges = 'do changes';
 const doChanges = 'do changes';
 const doChanges = 'do changes';
 const doChanges = 'do changes';
 const doChanges = 'do changes';
 const doChanges = 'do changes';
 const doChanges = 'do changes';
 const doChanges = 'do changes';
 const doChanges = 'do changes';
 const doChanges = 'do changes';
 const doChanges = 'do changes';
 const doChanges = 'do changes';
 const doChanges = 'do changes';
 const doChanges = 'do changes';
 const doChanges = 'do changes';
 const doChanges = 'do changes';
 const doChanges = 'do changes';
 const doChanges = 'do changes';
 const doChanges = 'do changes';
 const doChanges = 'do changes';
 const doChanges = 'do changes';
 const doChanges = 'do changes';