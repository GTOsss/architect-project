const defaultParams = {
  component: { path: 'src/page' },
  store: { path: 'src/store' },
};

const map = {
  stadiums: ['component', ['component', { rPath: '/test' }]],
};

module.exports = {
  defaultParams,
  map,
};
