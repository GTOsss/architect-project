import React, { memo } from 'react';
import styles from './styles.module.scss'

type Props = {
}

const myComponentName: React.FC<Props> = ({}) => {

  return ();
};

export default memo(myComponentName);
