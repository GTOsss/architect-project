// import chokidar from 'chokidar';
// import { resolve } from 'path';
// import configPath from '../configPath';
// import { memoRebuild } from './generators/createFileBySourceMap';
// import { smartRequire } from '../utils/smartRequire';
// import { Config } from '../types/config';

export const watchTemplateSettings = () => {
  // const config = smartRequire<Config>(configPath.config);
  // Object.entries(config.templates).forEach(([key, value]) => {
  // const watchPaths = value.watch.map((watchPath) => resolve(configPath.architect, watchPath));
  //
  // console.log(`watching directory: ${watchPaths}`);
  //
  // const watcher = chokidar.watch(watchPaths, {
  //   persistent: true,
  // });
  //
  // watcher.on('change', () => {
  //   memoRebuild(key);
  // });
  // });
};
