const defaultParams = {
  component: { path: 'src/page' },
  store: { path: 'src/store' },
};

const map = {
  stadiums: ['component', ['component', { name: 'custom' }]],
};

module.exports = {
  defaultParams,
  map,
};
